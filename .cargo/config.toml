# Cross-compilation configuration for static Linux musl binaries.
#
# On macOS, this requires the musl-cross toolchain:
#   brew install filosottile/musl-cross/musl-cross

[target.x86_64-unknown-linux-musl]
linker = "x86_64-linux-musl-gcc"
